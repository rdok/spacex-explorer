version: "3.7"

services:

  php:
      build:
         context: .
         dockerfile: ./docker/php/local.Dockerfile
         args:
           UID: $DEV_UID
      volumes:
        - .:/var/www:rw

  web:
      volumes:
        - .:/var/www:rw
